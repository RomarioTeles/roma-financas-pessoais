<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.roma.financaspessoais">

    <application
        android:name=".RomaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Romafinancaspessoais">
        <activity android:name=".ui.cadastros.CadastrarReceitaActivity"></activity>
        <activity android:name=".ui.cadastros.CadastrarDespesaActivity"></activity>
        <activity
            android:name=".ui.cadastros.ListarCategoriasActivity"
            android:label="@string/title_activity_listar_categorias" />
        <activity android:name=".ui.cadastros.CadastroCategoriaActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>